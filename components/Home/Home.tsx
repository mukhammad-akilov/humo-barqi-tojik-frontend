// Styles
import styles from './Home.module.scss';
import {Button, Container} from '@mui/material';

const Home = () : JSX.Element => {
    return (
       <section>
           <Container>
                Home page
           </Container>
       </section>
    );
};

export default Home;